#define NORMAL vec3(1.0)
#define MAKE_VEC3(x, y, z) vec3(x, y, z)
#define MAKE_VEC4(x, y, z, w) vec4(x, y, z, w)

struct Test {
    vec3 a;
    vec2 b;
    mat2 c;
};

struct Bruh {
    int a;
};

vec2 get_vec2(int b, vec4 c) {
    int a = b;
    return vec2(1.0);
}